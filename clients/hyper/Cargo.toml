[package]
name = "restless-hyper"
version = "0.1.0"
edition = "2021"

[dependencies]
http = "0.2.9"
hyper = "0.14.27"
restless-core = { workspace = true, features = ["http", "bytes"] }
thiserror.workspace = true
